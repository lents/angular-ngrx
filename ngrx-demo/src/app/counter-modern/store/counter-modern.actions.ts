import { createAction } from '@ngrx/store';

export const increment = createAction('[Counter Modern] Increment');
export const decrement = createAction('[Counter Modern] Decrement');
export const reset = createAction('[Counter Modern] Reset');
